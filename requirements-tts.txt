# TTS — choose ONE of the two options below:

# Option A: kokoro-onnx (recommended for NVIDIA GPU via ONNX Runtime)
#   After install, download model files:
#     wget https://github.com/thewh1teagle/kokoro-onnx/releases/download/model-files/kokoro-v0_19.onnx
#     wget https://github.com/thewh1teagle/kokoro-onnx/releases/download/model-files/voices.json
kokoro-onnx>=0.3.0
onnxruntime-gpu>=1.17.0   # GPU inference; use onnxruntime for CPU-only
soundfile>=0.12.0

# Option B: kokoro (PyTorch — requires torch with CUDA)
# kokoro>=0.9.2
# torch>=2.1.0            # install separately with CUDA wheel
# soundfile>=0.12.0
